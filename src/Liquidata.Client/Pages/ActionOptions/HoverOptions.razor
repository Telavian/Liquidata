@layout MainLayout
@using MudBlazor
@inherits HoverOptionsViewModel

<MudStack>
    <MudSlider Min="0" Max="5000" Step="250"
               @bind-Value="@TypedAction.WaitMilliseconds">
        Wait @(TypedAction.WaitMilliseconds) milliseconds
    </MudSlider>
</MudStack>